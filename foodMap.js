/* AQUÍ SE INTENTA COLOCAR UNA FUNCIÓN PARA PODER REALIZAR 
   LA VISTA SPLASH . RESULTADOS? FUNCIONARÁ PRÓXIMAMENTE. */
/* const foodMap = (start) => {
  const first = document.getElementById('preload');
  const loading = 0;
  const id = setInterval(frame, 64);
  if (loading == 120) {
    clearInterval(id);
    window.opener('index.html#user', '_self');
  } else {
    loading = loading + 1;
    if (loading == 90) {
      first.style.animation = 'fadeout 1s ease';
    }
  }
}; */